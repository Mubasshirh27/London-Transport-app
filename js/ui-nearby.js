(function() {  const UI = window.UI = window.UI || {};  function showPanel(panelId) {    const panel = document.getElementById(panelId);    panel.style.display = 'flex';    return panel;  }  let liveTracking = false;  UI.showNearbyStops = async function(lat, lon) {    const panel = document.getElementById('nearby-panel');    if (liveTracking && panel.style.display === 'none') return;    panel.style.display = 'flex';    const h3 = panel.querySelector('h3');    h3.innerHTML = liveTracking ? '📍 Nearby Stops <span class="live-badge">LIVE</span>' : '📍 Nearby Stops';    const list = document.getElementById('nearby-list');    list.innerHTML = '<div class="loading"><div class="spinner"></div></div>';    try {      const stops = await Stops.getNearby(lat, lon);      if (!stops.length) { list.innerHTML = '<div class="no-data">No stops nearby</div>'; return; }      const modeOrder = { bus: 0, tube: 1, 'national-rail': 2, 'elizabeth-line': 3, dlr: 4, overground: 5, tram: 6 };      stops.sort((a, b) => {        const mA = modeOrder[a.modes[0]] ?? 9, mB = modeOrder[b.modes[0]] ?? 9;        return mA - mB || a.distance - b.distance;      });      list.innerHTML = stops.slice(0, 20).map(s => {        const pm = s.modes.includes('bus') ? 'bus' : (s.modes[0] || '');        return `<div class="stop-item" data-stop-id="${s.id}" data-lat="${s.lat}" data-lon="${s.lon}">          <span class="stop-icon">${Stops.getModeIcon(pm)}</span>          <span class="stop-name">${s.name}</span>          <span class="stop-dist">${Math.round(s.distance)}m</span>          <span class="stop-modes">${s.modes.map(m => Stops.getModeIcon(m)).join('')}</span>        </div>`;      }).join('');      list.querySelectorAll('.stop-item').forEach(item => {        item.addEventListener('click', async () => {          UI.showDepartures(item.dataset.stopId, item.querySelector('.stop-name').textContent);          MapView.flyTo(parseFloat(item.dataset.lat), parseFloat(item.dataset.lon));        });      });      MapView.showStopMarkers(stops);    } catch { list.innerHTML = '<div class="no-data">Could not load stops</div>'; }    panel.querySelector('.panel-close').onclick = () => {      panel.style.display = 'none';    };  };  /* ── Live Tracking ── */  UI.setLiveTracking = function(on) {    liveTracking = on;    document.getElementById('live-toggle')?.classList.toggle('active', on);  };  UI.isLiveTracking = function() { return liveTracking; };  /* ── Bike markers toggle ── */  let bikeMarkersVisible = false;  UI.toggleBikeMarkers = function(val) {    bikeMarkersVisible = val !== undefined ? val : !bikeMarkersVisible;    return bikeMarkersVisible;  };  UI.isBikeMarkersVisible = function() { return bikeMarkersVisible; };  window.UI = UI;})();